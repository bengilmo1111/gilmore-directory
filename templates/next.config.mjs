/** @type {import('next').NextConfig} */
const nextConfig = {
  // Next.js basePath values have a leading slash but no trailing slash.
  basePath: '/GAME_SLUG',
  assetPrefix: '/GAME_SLUG',
};

export default nextConfig;
